CREATE TABLE `responds` (
	`id` INT NOT NULL,
	`A` VARCHAR NOT NULL,
	`B` VARCHAR NOT NULL,
	`C` VARCHAR NOT NULL,
	`D` VARCHAR NOT NULL,
	`E` VARCHAR NOT NULL,
	`F` VARCHAR NOT NULL,
	`G` VARCHAR NOT NULL,
	`H` VARCHAR NOT NULL,
	`I` VARCHAR NOT NULL,
	`J` VARCHAR NOT NULL,
	`K` VARCHAR NOT NULL,
	`L` VARCHAR NOT NULL,
	`M` VARCHAR NOT NULL,
	`N` VARCHAR NOT NULL,
	`O` VARCHAR NOT NULL,
	`P` VARCHAR NOT NULL,
	`Q` VARCHAR NOT NULL,
	`R` VARCHAR NOT NULL,
	`S` VARCHAR NOT NULL,
	`T` VARCHAR NOT NULL,
	`U` VARCHAR NOT NULL,
	`V` VARCHAR NOT NULL,
	`W` VARCHAR NOT NULL,
	`X` VARCHAR NOT NULL,
	`Y` VARCHAR NOT NULL,
	`Z` VARCHAR NOT NULL,
	`AA` VARCHAR NOT NULL,
	`AB` VARCHAR NOT NULL,
	`AC` VARCHAR NOT NULL,
	`AD` VARCHAR NOT NULL,
	`AE` VARCHAR NOT NULL,
	`AF` VARCHAR NOT NULL,
	`AG` VARCHAR NOT NULL,
	`AH` VARCHAR NOT NULL,
	`AI` VARCHAR NOT NULL,
	`AJ` VARCHAR NOT NULL,
	`AK` VARCHAR NOT NULL,
	`AL` VARCHAR NOT NULL,
	PRIMARY KEY (`id`)
);